spring:
  data:
    cassandra:
      keyspace-name: openklaster
      local-datacenter: datacenter1
      port: 9042
      contact-points: cassandra-db
      schemaAction: CREATE_IF_NOT_EXISTS
    mongodb:
      database: DEFAULT_DB
      port: 27017
      host: mongo-db
