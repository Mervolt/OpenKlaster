import { EqualsToDirective } from './equals-to.directive';

describe('NewPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualsToDirective();
    expect(directive).toBeTruthy();
  });
});
