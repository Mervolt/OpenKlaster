spring:
  profiles:
    active: EXTRACTOR_OFF
  data:
    cassandra:
      keyspace-name: openklaster
      local-datacenter: datacenter1
      port: 9042
      contact-points: localhost
      schemaAction: CREATE_IF_NOT_EXISTS
    mongodb:
      database: DEFAULT_DB
      port: 27017
      host: localhost
server:
  port: 8082
  servlet:
    context-path: /api/1
openklaster:
  files:
    base-path: ../file-repository/data
  mqtt:
    enabled: false
    topic: "-"
    address: "-"
    port: 1883
    cipher-key: "-"
    installation-id: "-"
